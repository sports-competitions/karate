<%= form_for(@qualification, role: "form") do |f| %>
  <% if @qualification.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@qualification.errors.count, "error") %> prohibited this qualification from being saved:</h2>

      <ul>
      <% @qualification.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :title %><br>
    <%= f.text_field :title, { class: "form-control" } %>
  </div>
  <div class="field">
    <%= f.label :view %><br>
    <%= f.select(:view, Qualification.views.keys, {}, { class: "form-control", id: "sel1" }) %>
  </div>
  <div class="field">
    <%= f.label :sport %><br>
    <%= f.collection_select(:sport_id, @sports, :id, :title, {}, { class: "form-control", id: "sel1" }) %>
  </div>
  <div class="actions">
    <%= f.submit class: "btn btn-success" %>
  </div>
<% end %>
